import React from "react";
import PropTypes from "prop-types";
import tw from "twin.macro";

function Header(props) {
  return (
    <div className="Header" tw="p-2">
      Header
    </div>
  );
}

Header.propTypes = {};

export default Header;
